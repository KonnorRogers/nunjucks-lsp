<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title></title>
  </head>
  <body>
    {{ data.foo }}
    {{ data.foo }}

    {{ foo | first }}

    {% for item in items %}
      {{ item.foo }}
    {% endfor %}
  </body>
</html>
